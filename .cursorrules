# AI 协作与商业逻辑约束 (Universal Rules)

## 核心业务：内衣跨境截流套利
本系统逻辑：从 TikTok 捕捉 77.75x 爆发信号 -> Amazon 搜索量/竞争校验
所有代码改动必须服务于此闭环，严禁引入任何冲淡该逻辑的“通用功能”。

---

## 🛑 强制改动流程 (Crucial Checklist)

在修改任何代码前，AI 必须严格执行以下三步：

1. **核对方案 (Approval First)**：
   - 禁止直接输出或修改代码。
   - 必须先以表格形式陈述：[修改目的]、[逻辑影响]、[对 SR 算法的影响]。
   - 得到用户明确指令“执行”后方可动工。

2. **同步账本 (Auto-Documentation)**：
   - 任何改动完成后，必须立即更新 `docs/business-rules.md`。
   - 记录格式：参考已有的数据格式

3. **保护核心公式**：
   - 严禁擅自修改涉及 `Momentum (爆发倍数)` 和 `Saturation (饱和度)` 的加权算法，除非用户要求优化。

---

## 🚫 拒绝策略
- 如果修改要求会导致 Amazon 竞争数（Results）被忽略，必须强制提醒用户。

---
**确认指令**：读取此文件后，请回复“业务逻辑已锁定，改动前必核对”。
